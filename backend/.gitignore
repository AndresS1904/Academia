node_modules
# Keep environment variables out of version control (all variants), but allow example templates
.env*
!.env*.example

/generated/prisma

# Build artifacts and runtime data — never commit
/dist
/uploads
*.log
*.tsbuildinfo
/coverage
